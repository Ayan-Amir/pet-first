"""Local PetsFirst REST API mock for agent / MCP development.

Run:
    python -m mock_backend

Point the app at it (no OAuth needed when client id/secret are empty):
    PETSFIRST_BASE_URL=http://127.0.0.1:8020
    PETSFIRST_MCP_API_PREFIX=/api/v1.0/mcp
"""
